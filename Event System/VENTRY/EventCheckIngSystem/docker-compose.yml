services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: event_db
      POSTGRES_USER: Luchistack
      POSTGRES_PASSWORD: '@Ikorodu2025!'
    ports:
      - "5432:5432"